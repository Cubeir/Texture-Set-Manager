<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="Texture_Set_Manager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="using:Texture_Set_Manager"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="Texture Set Manager"
        mc:Ignorable="d">

    <Window.SystemBackdrop>
        <DesktopAcrylicBackdrop />
    </Window.SystemBackdrop>

    <Grid x:Name="MainGrid">

        <!-- Splash Blocker -->
        <Grid x:Name="SplashOverlay"
              Grid.ColumnSpan="2"
              Canvas.ZIndex="9999">
            <Grid.Background>
                <SolidColorBrush Color="{ThemeResource SolidBackgroundFillColorBase}" />
            </Grid.Background>

            <Viewbox HorizontalAlignment="Stretch"
                     VerticalAlignment="Stretch"
                     Margin="128, 64, 128, 64"
                     Stretch="Uniform">
                <Grid Width="256"
                      Height="256">
                    <Image x:Name="SplashLamp"
                           Source="ms-appx:///Assets/icon.png"
                           Width="192"
                           Height="192"
                           Opacity="1.0"
                           Stretch="Uniform"
                           Canvas.ZIndex="2"
                           CacheMode="BitmapCache" />
                </Grid>
            </Viewbox>
        </Grid>

        <!-- Light theme tint overlay -->
        <Border x:Name="LightThemeOverlay"
                Grid.ColumnSpan="2"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                IsHitTestVisible="False"
                Visibility="{ThemeResource LightThemeBackgroundVisibility}"
                Canvas.ZIndex="-999">
            <Border.Background>
                <SolidColorBrush Color="#20373737" />
            </Border.Background>
        </Border>

        <!-- Titlebar background -->
        <Grid Grid.ColumnSpan="2"
              HorizontalAlignment="Stretch"
              VerticalAlignment="Top"
              Height="35">
            <!-- Shadow receiver behind the titlebar) -->
            <Grid x:Name="TitleBarShadowReceiver" />
            <!-- Actual titlebar -->
            <Border x:Name="TitleBarBackground"
                    Height="37"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Top"
                    Background="{ThemeResource CardBackgroundFillColorSecondary}"
                    BorderBrush="{ThemeResource HyperlinkButtonBorderThemeBrush}"
                    BorderThickness="0,0,0,6"
                    CornerRadius="0"
                    Translation="0,0,32"
                    IsHitTestVisible="False">
                <Border.Shadow>
                    <ThemeShadow x:Name="TitleBarShadow" />
                </Border.Shadow>
            </Border>

            <!-- Transparent drag area: starts past the titlebar buttons so they keep receiving clicks -->
            <Border x:Name="TitleBarDragArea"
                    Margin="197,-1,0,0"
                    Height="32"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Top"
                    BorderThickness="0"
                    Background="Transparent"
                    IsHitTestVisible="False" />
        </Grid>

        <!-- Titlebar Center Content -->
        <Grid Grid.ColumnSpan="2"
              HorizontalAlignment="Center"
              VerticalAlignment="Top"
              Margin="0, -1, -1, 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- A Grid rather than a Canvas so the image is laid out dead-center of its own
                 box; combined with RenderTransformOrigin 0.5,0.5 the spin axis is the exact
                 middle of the icon at any size or DPI. BitmapCache is deliberately absent:
                 caching rasterizes once and then transforms that snapshot, which is what made
                 the logo look like it sagged off-axis once it picked up speed. -->
            <Grid Grid.Column="0"
                  Height="20"
                  Width="20"
                  VerticalAlignment="Center"
                  Margin="0,1,8,0"
                  Canvas.ZIndex="1">

                <!-- icon -->
                <Image x:Name="iconImageBox"
                       Source="ms-appx:///Assets/icon.png"
                       Height="20"
                       Width="20"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Opacity="1.0"
                       RenderTransformOrigin="0.5,0.5"
                       Canvas.ZIndex="3"
                       IsHitTestVisible="False" />
            </Grid>

            <TextBlock x:Name="TitleBarText"
                       Text="Texture Set Manager"
                       Grid.Column="1"
                       FontSize="14"
                       Margin="0,7,0,5"
                       VerticalAlignment="Center"
                       Canvas.ZIndex="0"
                       FontWeight="Medium"
                       IsTextScaleFactorEnabled="False"
                       ToolTipService.ToolTip="Version: Unknown"
                       IsHitTestVisible="True" />
        </Grid>



        <!-- CONTENT AREA -->
        <Grid x:Name="ContentGrid">

            <!-- Titlebar Buttons -->
            <Border x:Name="CommandBarButtonsShadowReceiver"
                    Margin="-2,-2,16,0"
                    VerticalAlignment="Top"
                    Height="34"
                    Width="Auto"
                    Translation="0,0,0"
                    CornerRadius="4">
                <Border.Shadow>
                    <ThemeShadow x:Name="CommandBarShadow" />
                </Border.Shadow>
                <Grid Background="Transparent"
                      BorderBrush="Transparent"
                      BorderThickness="1"
                      CornerRadius="4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <!-- Button -->
                        <ColumnDefinition Width="Auto" />
                        <!-- Separator -->
                        <ColumnDefinition Width="Auto" />
                        <!-- Button -->
                        <ColumnDefinition Width="Auto" />
                        <!-- Separator -->
                        <ColumnDefinition Width="Auto" />
                        <!-- Button -->
                        <ColumnDefinition Width="Auto" />
                        <!-- Separator -->
                        <ColumnDefinition Width="Auto" />
                        <!-- Button -->
                    </Grid.ColumnDefinitions>

                    <!-- Chat Button -->
                    <Button x:Name="ChatButton"
                            Grid.Column="0"
                            FontFamily="{ThemeResource SymbolThemeFontFamily}"
                            Style="{ThemeResource SubtleButtonStyle}"
                            Content="&#xE8F2;"
                            Width="42"
                            Height="35"
                            CornerRadius="0"
                            FontSize="14"
                            Background="Transparent"
                            BorderBrush="Transparent"
                            Click="ChatButton_Click"
                            ToolTipService.ToolTip="Invitation to the Vanilla RTX Discord server"
                            IsTextScaleFactorEnabled="False" />

                    <!-- Separator -->
                    <Border Grid.Column="1"
                            Width="1"
                            Margin="4,6,4,6"
                            Background="{ThemeResource DividerStrokeColorDefaultBrush}" />

                    <!-- Donate Button -->
                    <Button x:Name="DonateButton"
                            Grid.Column="2"
                            FontFamily="{ThemeResource SymbolThemeFontFamily}"
                            Style="{StaticResource SubtleButtonStyle}"
                            Content="&#xEB51;"
                            Width="42"
                            Height="35"
                            CornerRadius="0"
                            FontSize="14"
                            Background="Transparent"
                            BorderBrush="Transparent"
                            Click="DonateButton_Click"
                            PointerEntered="DonateButton_PointerEntered"
                            PointerExited="DonateButton_PointerExited"
                            ToolTipService.ToolTip="Buy the developer some coffee.
                            (makes him really happy!)"
                            IsTextScaleFactorEnabled="False" />

                    <!-- Separator -->
                    <Border Grid.Column="3"
                            Width="1"
                            Margin="4,6,4,6"
                            Background="{ThemeResource DividerStrokeColorDefaultBrush}" />

                    <!-- Theme Button -->
                    <Button x:Name="CycleThemeButton"
                            Grid.Column="4"
                            FontFamily="{ThemeResource SymbolThemeFontFamily}"
                            Style="{StaticResource SubtleButtonStyle}"
                            Content="&#xEC46;"
                            Width="42"
                            Height="35"
                            CornerRadius="0"
                            FontSize="16"
                            Background="Transparent"
                            BorderBrush="Transparent"
                            Click="CycleThemeButton_Click"
                            ToolTipService.ToolTip="Theme: System"
                            IsTextScaleFactorEnabled="False" />

                    <!-- Separator -->
                    <Border Grid.Column="5"
                            Width="1"
                            Margin="4,6,4,6"
                            Background="{ThemeResource DividerStrokeColorDefaultBrush}" />

                    <!-- Texture Set Tools -->
                    <Button x:Name="TextureSetToolsButton"
                            Grid.Column="6"
                            FontFamily="{ThemeResource SymbolThemeFontFamily}"
                            Style="{StaticResource SubtleButtonStyle}"
                            Content="&#xEC7A;"
                            Width="42"
                            Height="35"
                            CornerRadius="0"
                            FontSize="16"
                            Background="Transparent"
                            BorderBrush="Transparent"
                            ToolTipService.ToolTip="Other texture set management tools"
                            IsTextScaleFactorEnabled="False">
                        <Button.Flyout>
                            <MenuFlyout Placement="Bottom">
                                <MenuFlyoutItem x:Name="AnalyzeTextureSetsMenuItem"
                                                Text="Analyze texture sets"
                                                Click="AnalyzeTextureSetsMenuItem_Click"
                                                ToolTipService.ToolTip="Pick a folder and report every PBR texture that is still identical to its color texture (most likely an untouched template). Always searches subfolders. Reads only, changes nothing.">
                                    <MenuFlyoutItem.Icon>
                                        <FontIcon Glyph="&#xE9D9;" />
                                    </MenuFlyoutItem.Icon>
                                </MenuFlyoutItem>

                                <MenuFlyoutSeparator />

                                <MenuFlyoutItem x:Name="StripTextureSetsMenuItem"
                                                Text="Strip all texture sets"
                                                Click="StripTextureSetsMenuItem_Click"
                                                ToolTipService.ToolTip="Pick a folder and delete every texture set along with the MER/MERS, normal and heightmap textures they reference, leaving only the color textures. You will be asked to choose a scope before anything is deleted.">
                                    <MenuFlyoutItem.Icon>
                                        <FontIcon Glyph="&#xE74D;" />
                                    </MenuFlyoutItem.Icon>
                                </MenuFlyoutItem>
                            </MenuFlyout>
                        </Button.Flyout>
                    </Button>
                </Grid>
            </Border>

            <!-- Controls Grid -->
            <Grid x:Name="ControlsGrid"
                  Margin="16,56,16,16"
                  VerticalAlignment="Stretch"
                  HorizontalAlignment="Stretch">

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>


                <!-- Select Folders / Select Files & Clear Split Button -->
                <Border x:Name="SelectFolderButtonsShadowHost"
                        Grid.Row="0"
                        Margin="0, 0 ,0 ,12"
                        VerticalAlignment="Top"
                        Height="60"
                        Translation="0,0,24"
                        CornerRadius="4">
                    <Border.Shadow>
                        <ThemeShadow x:Name="SelectFolderButtonsShadow" />
                    </Border.Shadow>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*" />
                            <ColumnDefinition Width="6" />
                            <ColumnDefinition Width="1*" />
                            <ColumnDefinition Width="6" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Button x:Name="SelectFolderButton"
                                Grid.Column="0"
                                Click="SelectFolderButton_Click"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Stretch"
                                CornerRadius="4,0,0,4"
                                BorderThickness="0"
                                IsTextScaleFactorEnabled="False"
                                ToolTipService.ToolTip="Select folder containing textures (overwrites previous folder selection)"
                                AllowDrop="True"
                                DragOver="SelectFolderButton_DragOver"
                                Drop="SelectFolderButton_Drop">
                            <StackPanel Orientation="Horizontal"
                                        HorizontalAlignment="Center">
                                <FontIcon Glyph="&#xE8B7;"
                                          FontSize="16"
                                          Margin="0,0,8,0"
                                          VerticalAlignment="Center" />
                                <TextBlock Text="Select folder"
                                           FontWeight="SemiBold"
                                           VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>

                        <Button x:Name="SelectFilesButton"
                                Grid.Column="2"
                                Click="SelectFilesButton_Click"
                                FontWeight="SemiBold"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Stretch"
                                CornerRadius="0,0,0,0"
                                BorderThickness="0"
                                IsTextScaleFactorEnabled="False"
                                ToolTipService.ToolTip="Select individual texture files (overwrites previous file selections)"
                                AllowDrop="True"
                                DragOver="SelectFilesButton_DragOver"
                                Drop="SelectFilesButton_Drop">
                            <StackPanel Orientation="Horizontal"
                                        HorizontalAlignment="Center">
                                <FontIcon Glyph="&#xF56E;"
                                          FontSize="16"
                                          Margin="0,0,8,0"
                                          VerticalAlignment="Center" />
                                <TextBlock Text="Select files"
                                           FontWeight="SemiBold"
                                           VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>

                        <!-- Clear Selection Button -->
                        <Button x:Name="ClearSelectionButton"
                                Grid.Column="4"
                                Click="ClearSelectionButton_Click"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Stretch"
                                Width="60"
                                CornerRadius="0,4,4,0"
                                BorderThickness="0"
                                IsTextScaleFactorEnabled="False"
                                ToolTipService.ToolTip="Clear file and folder selections manually">
                            <FontIcon Glyph="&#xEA99;"
                                      FontSize="18"
                                      VerticalAlignment="Center" />
                        </Button>

                        <!-- Overlay borders for the visual split -->
                        <Grid Grid.Column="0"
                              HorizontalAlignment="Right"
                              Width="3"
                              Margin="0,0,-3,0"
                              IsHitTestVisible="False">
                            <Rectangle Fill="{ThemeResource ButtonBackgroundThemeBrush}" />
                            <Border BorderBrush="{ThemeResource FakeSplitButtonDarkBorderColor}"
                                    BorderThickness="3,0,0,0" />
                        </Grid>

                        <Grid Grid.Column="2"
                              HorizontalAlignment="Left"
                              Width="3"
                              Margin="-3,0,0,0"
                              IsHitTestVisible="False">
                            <Rectangle Fill="{ThemeResource ButtonBackgroundThemeBrush}" />
                            <Border BorderBrush="{ThemeResource FakeSplitButtonBrightBorderColor}"
                                    BorderThickness="3,0,0,0" />
                        </Grid>

                        <Grid Grid.Column="4"
                              HorizontalAlignment="Left"
                              Width="3"
                              Margin="-3,0,0,0"
                              IsHitTestVisible="False">
                            <Rectangle Fill="{ThemeResource ButtonBackgroundThemeBrush}" />
                            <Border BorderBrush="{ThemeResource FakeSplitButtonBrightBorderColor}"
                                    BorderThickness="3,0,0,0" />
                        </Grid>

                        <Grid Grid.Column="2"
                              HorizontalAlignment="Right"
                              Width="3"
                              Margin="0,0,-3,0"
                              IsHitTestVisible="False">
                            <Rectangle Fill="{ThemeResource ButtonBackgroundThemeBrush}" />
                            <Border BorderBrush="{ThemeResource FakeSplitButtonDarkBorderColor}"
                                    BorderThickness="3,0,0,0" />
                        </Grid>

                    </Grid>
                </Border>


                <!-- Secondary PBR Map Dropdown and SSS Toggle -->
                <Border x:Name="SecondaryPBRMapShadowHost"
                        Grid.Row="1"
                        Margin="0,4,0,0"
                        VerticalAlignment="Top"
                        Height="40"
                        Translation="0,0,22"
                        CornerRadius="4">
                    <Border.Shadow>
                        <ThemeShadow x:Name="SecondaryPBRMapShadow" />
                    </Border.Shadow>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*" />
                            <ColumnDefinition Width="6" />
                            <!-- 4px gap between buttons -->
                            <ColumnDefinition Width="0.6*" />
                        </Grid.ColumnDefinitions>

                        <DropDownButton x:Name="SecondaryPBRMapDropDown"
                                        Grid.Column="0"
                                        Content="Secondary PBR Map"
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Stretch"
                                        FontWeight="SemiBold"
                                        CornerRadius="4,0,0,4"
                                        BorderThickness="0"
                                        IsTextScaleFactorEnabled="False"
                                        ToolTipService.ToolTip="Select secondary PBR map type">
                            <DropDownButton.Flyout>
                                <MenuFlyout Placement="Bottom">
                                    <MenuFlyoutItem Text="None"
                                                    Click="SecondaryPBRMapOption_Click" />
                                    <MenuFlyoutItem Text="Normal map"
                                                    Click="SecondaryPBRMapOption_Click" />
                                    <MenuFlyoutItem Text="Heightmap"
                                                    Click="SecondaryPBRMapOption_Click" />
                                </MenuFlyout>
                            </DropDownButton.Flyout>
                        </DropDownButton>

                        <!-- SSS Toggle -->
                        <ToggleButton x:Name="IncludeSubsurfaceScatteringToggle"
                                      Grid.Column="2"
                                      Content="Subsurface scattering"
                                      ToolTipService.ToolTip="Enable Subsurface Scattering property in the texture sets."
                                      Checked="IncludeSubsurfaceScatteringToggle_Checked"
                                      Unchecked="IncludeSubsurfaceScatteringToggle_Unchecked"
                                      HorizontalAlignment="Stretch"
                                      VerticalAlignment="Stretch"
                                      FontWeight="SemiBold"
                                      CornerRadius="0,4,4,0"
                                      BorderThickness="0"
                                      Height="40"
                                      Margin="0,0,0,0"
                                      IsTextScaleFactorEnabled="False">
                            <ToggleButton.ContentTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal"
                                                HorizontalAlignment="Center">
                                        <FontIcon Glyph=""
                                                  FontSize="16"
                                                  Margin="0,0,8,0"
                                                  VerticalAlignment="Center" />
                                        <TextBlock Text="Subsurface scattering"
                                                   FontWeight="SemiBold"
                                                   VerticalAlignment="Center" />
                                    </StackPanel>
                                </DataTemplate>
                            </ToggleButton.ContentTemplate>
                        </ToggleButton>

                        <!-- Overlay borders for the visual split. Each half of the seam belongs to
                             the button it touches and is driven from code (ApplySSSBevelColors) so it
                             dims when that button is disabled and follows the theme. Only the SSS
                             side takes the accent when the toggle is checked - the other half is the
                             dropdown's edge, and the dropdown has no accented state to represent. -->
                        <!-- Dark border on RIGHT edge of the dropdown -->
                        <Grid Grid.Column="0"
                              HorizontalAlignment="Right"
                              Width="3"
                              Margin="0,0,-3,0"
                              IsHitTestVisible="False">
                            <Rectangle Fill="{ThemeResource ButtonBackgroundThemeBrush}" />
                            <Border BorderBrush="{ThemeResource FakeSplitButtonDarkBorderColor}"
                                    BorderThickness="3,0,0,0"
                                    x:Name="RightEdgeOfSecondaryPBRDropDown" />
                        </Grid>

                        <!-- Bright border on LEFT edge of the SSS button -->
                        <Grid Grid.Column="2"
                              HorizontalAlignment="Left"
                              Width="3"
                              Margin="-3,0,0,0"
                              IsHitTestVisible="False">
                            <Rectangle Fill="{ThemeResource ButtonBackgroundThemeBrush}" />
                            <Border BorderBrush="{ThemeResource FakeSplitButtonBrightBorderColor}"
                                    BorderThickness="3,0,0,0"
                                    x:Name="LeftEdgeOfSSSButton" />
                        </Grid>
                    </Grid>
                </Border>

                <!-- Toggle Switches -->
                <Grid Grid.Row="2"
                      Margin="0,18,0,0"
                      HorizontalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.25*" />
                        <ColumnDefinition Width="1*" />
                        <ColumnDefinition Width="1*" />
                        <ColumnDefinition Width="1*" />
                        <ColumnDefinition Width="1.25*" />
                    </Grid.ColumnDefinitions>

                    <ToggleSwitch x:Name="ProcessSubfoldersToggle"
                                  Margin="4"
                                  Grid.Column="0"
                                  Header="Process subfolders"
                                  ToolTipService.ToolTip="Enable to process subfolders of the selected folder."
                                  IsOn="True"
                                  OffContent=""
                                  OnContent=""
                                  Toggled="ProcessSubfoldersToggle_Toggled"
                                  IsTextScaleFactorEnabled="False" />

                    <ToggleSwitch x:Name="SmartFiltersToggle"
                                  Margin="4"
                                  Grid.Column="1"
                                  Header="Smart filters"
                                  ToolTipService.ToolTip="Enables prevention measures against re-processing existing textures that likely belong to a texture set or use common PBR texture suffixes, recommended to leave on."
                                  IsOn="True"
                                  OffContent=""
                                  OnContent=""
                                  Toggled="SmartFiltersToggle_Toggled"
                                  IsTextScaleFactorEnabled="False" />

                    <ToggleSwitch x:Name="ConvertToTGAToggle"
                                  Margin="4"
                                  Grid.Column="2"
                                  Header="Convert to TGA"
                                  ToolTipService.ToolTip="If enabled, newly generated PBR texture sets will be in Targa format (32 bit, 8 bit per channel, ARGB colorspace, no compression)"
                                  IsOn="True"
                                  OffContent=""
                                  OnContent=""
                                  Toggled="ConvertToTGAToggle_Toggled"
                                  IsTextScaleFactorEnabled="False" />

                    <ToggleSwitch x:Name="CreateBackupToggle"
                                  Margin="4"
                                  Grid.Column="3"
                                  Header="Create backup"
                                  ToolTipService.ToolTip="Enable to save backups of your selected files and folders before processing begins."
                                  IsOn="true"
                                  OffContent=""
                                  OnContent=""
                                  Toggled="CreateBackupToggle_Toggled"
                                  IsTextScaleFactorEnabled="False" />

                    <ToggleSwitch x:Name="CreateNewFoldersToggle"
                                  Margin="4"
                                  Grid.Column="4"
                                  Header="Create new folders"
                                  ToolTipService.ToolTip="Enable to collect the generated texture set JSONs and PBR templates in a new subfolder next to each processed folder (Heightmaps-PBR, Normals-PBR or Flat-PBR, depending on the secondary PBR texture) instead of writing them alongside the color textures."
                                  IsOn="False"
                                  OffContent=""
                                  OnContent=""
                                  Toggled="CreateNewFoldersToggle_Toggled"
                                  IsTextScaleFactorEnabled="False" />
                </Grid>

            </Grid>

            <!-- Generate button, the bevelled seam under it, and the log area.
                 One grid with real rows rather than four siblings each pinned by a hand-tuned
                 top margin (275 / 315 / 318 / 321) inside a row-less grid: the button's height
                 now drives where the seam and log start, so resizing the button doesn't mean
                 re-deriving three other numbers by hand. Renders identically. -->
            <Grid Margin="16,275,16,16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <!-- Generate button -->
                    <RowDefinition Height="3" />
                    <!-- Dark half of the bevelled seam -->
                    <RowDefinition Height="3" />
                    <!-- Bright half -->
                    <RowDefinition Height="*" />
                    <!-- Log fills whatever is left -->
                </Grid.RowDefinitions>

                <!-- Generate Button -->
                <Button x:Name="GenerateButton"
                        Grid.Row="0"
                        ToolTipService.ToolTip="Begin generating PBR texture sets based on the selected folders and given parameters."
                        Click="GenerateButton_Click"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"
                        CornerRadius="5,5,0,0"
                        BorderThickness="0"
                        Height="46">
                    <Button.ContentTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal"
                                        HorizontalAlignment="Center">
                                <FontIcon Glyph="&#xF4A5;"
                                          FontSize="16"
                                          Margin="0,0,8,0"
                                          VerticalAlignment="Center" />
                                <TextBlock Text="Generate"
                                           FontWeight="SemiBold"
                                           VerticalAlignment="Center" />
                            </StackPanel>
                        </DataTemplate>
                    </Button.ContentTemplate>
                    <Button.Shadow>
                        <ThemeShadow x:Name="GenerateButtonShadow" />
                    </Button.Shadow>
                </Button>

                <!-- Bevelled seam between the button and the log -->
                <Grid Grid.Row="1"
                      IsHitTestVisible="False">
                    <Rectangle Fill="{ThemeResource ButtonBackgroundThemeBrush}" />
                    <Border BorderBrush="{ThemeResource FakeSplitButtonDarkBorderColor}"
                            BorderThickness="0,3,0,0" />
                </Grid>

                <Grid Grid.Row="2"
                      IsHitTestVisible="False">
                    <Rectangle Fill="{ThemeResource ButtonBackgroundThemeBrush}" />
                    <Border BorderBrush="{ThemeResource FakeSplitButtonBrightBorderColor}"
                            BorderThickness="0,3,0,0" />
                </Grid>

                <!-- Log area: shadow host, the textbox itself, the busy indicator, and the
                     invisible click-to-copy button laid over it -->
                <Border x:Name="SidebarLogShadowHost"
                        Grid.Row="3"
                        Translation="0,0,24"
                        BorderThickness="0"
                        CornerRadius="6">
                    <Border.Shadow>
                        <ThemeShadow x:Name="SidebarLogShadow" />
                    </Border.Shadow>
                </Border>

                <TextBox x:Name="SidebarLog"
                         Grid.Row="3"
                         BorderThickness="0"
                         TextWrapping="Wrap"
                         IsTabStop="False"
                         TextAlignment="Center"
                         HorizontalTextAlignment="Center"
                         VerticalContentAlignment="Center"
                         AcceptsReturn="True"
                         IsReadOnly="True"
                         Padding="0,8,0,0"
                         AllowFocusOnInteraction="True"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         IsTextScaleFactorEnabled="False"
                         ToolTipService.ToolTip="Relevant logs, click to copy!"
                         CornerRadius="0,0,6,6" />

                <!-- Busy indicator. Collapsed unless something long-running is in flight, so it
                     costs nothing visually the rest of the time; overlaid rather than given its
                     own row so the log doesn't jump when work starts and stops. -->
                <ProgressBar x:Name="SidelogProgressBar"
                             Grid.Row="3"
                             Margin="12,4,12,0"
                             VerticalAlignment="Top"
                             HorizontalAlignment="Stretch"
                             Canvas.ZIndex="1000"
                             IsIndeterminate="False"
                             ShowPaused="False"
                             ShowError="False"
                             Visibility="Collapsed" />

                <!-- Right margin leaves the log's own scrollbar clickable -->
                <Button x:Name="LogCopyButton"
                        Grid.Row="3"
                        Background="Transparent"
                        BorderBrush="Transparent"
                        VerticalAlignment="Stretch"
                        HorizontalAlignment="Stretch"
                        Margin="0,0,16,0"
                        Opacity="0.3"
                        BorderThickness="0"
                        Canvas.ZIndex="999"
                        CornerRadius="0,0,0,6"
                        IsDoubleTapEnabled="True"
                        Click="LogCopyButton_Click" />
            </Grid>
        </Grid>


    </Grid>
</Window>
